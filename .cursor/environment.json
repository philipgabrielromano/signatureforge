{
  "name": "SignatureForge",
  "install": "bash scripts/cloud-agent-install.sh",
  "start": "bash scripts/cloud-agent-start.sh",
  "terminals": [
    {
      "name": "dev-server",
      "command": "set -a && . ./.env && set +a && npm run dev",
      "description": "Next.js dev server on http://localhost:43123 (AUTH_DEMO_MODE=true)."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 43123
    }
  ]
}
